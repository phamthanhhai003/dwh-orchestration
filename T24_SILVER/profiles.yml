t24_silver:
  outputs:
    dev:
      type: dremio
      object_storage_source: 'hive'
      dremio_space: '@user'
      schema: 'silver'
      software_host: "{{ env_var('DREMIO_HOST') }}"
      port: 9047
      use_ssl: false
      user: "{{ env_var('DREMIO_USER') }}"
      password: "{{ env_var('DREMIO_PASSWORD') }}"
      threads: 4
  target: dev
