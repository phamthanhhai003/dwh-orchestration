apiVersion: "sparkoperator.k8s.io/v1beta2"
kind: SparkApplication
metadata:
  name: "hold-acct-parser-{{ params.slug }}-job"
  namespace: "{{ var.value.get('SPARK_NAMESPACE', 'spark2-development-ns') }}"
spec:
  type: Python
  mode: cluster
  image: "congtvjits/spark-t24:build-182"
  imagePullPolicy: IfNotPresent
  pythonVersion: "3"
  mainApplicationFile: "local:///opt/jobs/accounting_parser_spark.py"

  # CRF/CRC: GL/PL text (DistributedGLParser tự nhận CRF vs CRC qua header 'LIVE SYSTEM').
  # Input = raw/hold/<REPORT_NAME>/business_date=<D>/<recid>; partition business_date (khớp reconcile_seal).
  arguments:
    - "--bucket"
    - "{{ var.value.get('RAW_BUCKET', 'raw') }}"
    - "--prefix"
    - "hold/{{ params.report_name }}/business_date="
    - "--date"
    - "{{ ti.xcom_pull(task_ids='detect', key='business_date') }}"
    - "--table"
    - "{{ params.bronze }}"
    - "--partition-col"
    - "business_date"

  sparkVersion: "3.5.1"
  sparkConf:
    "spark.driver.extraClassPath": "/opt/spark/jars/*"
    "spark.executor.extraClassPath": "/opt/spark/jars/*"
    "spark.sql.extensions": "org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions"
    "spark.sql.catalog.hive": "org.apache.iceberg.spark.SparkCatalog"
    "spark.sql.catalog.hive.type": "hive"
    "spark.sql.catalog.hive.uri": "{{ var.value.HIVE_METASTORE_URI }}"
    "spark.sql.catalog.hive.warehouse": "s3a://hive-warehouse"
    "spark.sql.defaultCatalog": "hive"
    "spark.sql.warehouse.dir": "s3a://hive-warehouse"
    "spark.sql.catalog.hive.io-impl": "org.apache.iceberg.aws.s3.S3FileIO"
    "spark.sql.catalog.hive.s3.endpoint": "{{ conn.minio_conn.extra_dejson.endpoint_url }}"
    "spark.sql.catalog.hive.s3.path-style-access": "true"
    "spark.sql.catalog.hive.client.region": "us-east-1"
    "spark.sql.catalog.hive.s3.access-key-id": "{{ conn.minio_conn.login }}"
    "spark.sql.catalog.hive.s3.secret-access-key": "{{ conn.minio_conn.password }}"
    "spark.hadoop.fs.s3a.endpoint": "{{ conn.minio_conn.extra_dejson.endpoint_url }}"
    "spark.hadoop.fs.s3a.access.key": "{{ conn.minio_conn.login }}"
    "spark.hadoop.fs.s3a.secret.key": "{{ conn.minio_conn.password }}"
    "spark.hadoop.fs.s3a.path.style.access": "true"
    "spark.hadoop.fs.s3a.impl": "org.apache.hadoop.fs.s3a.S3AFileSystem"
  driver:
    cores: 1
    coreLimit: "1200m"
    memory: "1500m"
    serviceAccount: spark-sa
  executor:
    cores: 1
    coreLimit: "1200m"
    instances: 1
    memory: "1500m"
  restartPolicy:
    type: Never
